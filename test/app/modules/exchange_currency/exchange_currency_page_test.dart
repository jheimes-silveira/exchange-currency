import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ExchangeCurrencyPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ExchangeCurrencyPage(title: 'ExchangeCurrency')));
    //  final titleFinder = find.text('ExchangeCurrency');
    //  expect(titleFinder, findsOneWidget);
  });
}
